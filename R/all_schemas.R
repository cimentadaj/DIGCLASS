#' A list with all available translations between occupational schemas.
#'
#' Each slot contains a data frame for each of the translation between two schemas. The
#' name of each slot in the list shows which schemas being translated. Always, the
#' first column is the origin schema and the second column is the destination schema.
#'
#' @name all_schemas
#' @format list
NULL
